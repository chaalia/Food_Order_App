import classes from './MealItemForm.module.css';

const MealItemForm = () => {
    return (
        <form className={classes.forms}>
            <input />
            <button>+ Add</button>
        </form>
    );
}

export default MealItemForm;